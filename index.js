require('angular');
require('./src'); //this is to require my-widget and all its components

module.exports = 'authClient'//this is the name of the angularjs module that 'contains' all of the controllers and directives
